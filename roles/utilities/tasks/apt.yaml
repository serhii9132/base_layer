---
- name: Apt update and upgrade
  ansible.builtin.apt:
    update_cache: true
    upgrade: "yes"

- name: Install packages
  loop: "{{ __utilities_core + utilities_extra }}"
  ansible.builtin.apt:
    name: "{{ item }}"
